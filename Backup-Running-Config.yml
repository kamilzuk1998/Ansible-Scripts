---
- name: 'ASA config'
  hosts: ASAs
  connection: local
  gather_facts: false
  tasks:
    - name: Retrieve config files from ASAs
      asa_command:
        commands:
          - show run
        provider:
          username: "kamil.zuk"
          password: "January18"
      when: status != 'build'
      register: output
    - copy: 
        content: "{{ output.stdout[0] }}"
        dest: "/etc/ansible/show_run_{{ inventory.hostname }}.txt"
